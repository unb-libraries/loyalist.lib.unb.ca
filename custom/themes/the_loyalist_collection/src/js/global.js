/**
 * @file
 * Global lib_unb_ca JS functions.
 */
(function($, Drupal) {
    'use strict';

    Drupal.behaviors.loyalist_global = {
        attach: function (context, settings) {
            // JS feature code.
        }
    };
})(jQuery, Drupal);